{
  "name": "dalcove-portfolio",
  "version": "2.0.0",
  "description": "Dalcove Portfolio - lightweight static site",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev": "esbuild src/js/main.js --bundle --minify --outdir=dist --inject:src/styles/styles.css"
  },
  "dependencies": {
    "lenis": "^1.0.30"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.14",
    "esbuild": "^0.17.3",
    "postcss": "^8.4.24",
    "tailwindcss": "^3.3.0"
  }
}